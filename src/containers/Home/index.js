import React from "react"
import Header from "../../components/Header";

const Home = () => {
	return <div>
		<Header/>
		<div>this is dell</div>
		<button onClick={() => alert("click1")}>btn</button>
	</div>
}

export default Home
